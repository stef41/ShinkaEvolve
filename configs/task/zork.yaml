# Zork task configuration for ShinkaEvolve

evaluate_function:
  _target_: examples.zork.evaluate.evaluate_zork_agent
  program_path: ???
  results_dir: ???

distributed_job_config:
  _target_: shinka.launch.LocalJobConfig
  eval_program_path: "shinka/eval_hydra.py"

evo_config:
  task_sys_msg: >
    Evolve Zork agents to maximize score (0-350 points). Agent: predict(observation) -> action.
    Key: Find treasures, open containers, deposit in trophy case, explore efficiently.
    Track state, avoid loops, prioritize: treasure > container > explore.
  language: "python"
  init_program_path: "examples/zork/initial.py"
  job_type: "local"

exp_name: "shinka_zork"
